The scripts in this directory whose names begin with an 'S' are executed when starting the container.

To disable a script in this directory, rename it so that it begins with a 'K'

By default, scripts are run as bash scripts. Use a hashbang at the top of the script to use
something other than bash.

All script files need to be executable.

Airstack CLI adds the following files with contents from the .airstack.yml config file.
- S45start_before
- S50start_cmd
- S55start_after
- S90stop
